#include "main.h"

/**
 * print_to_98 - print numbers. n-98
 * @n: num to count from
 */

void print_to_98(int n)
{
    for (i = n; i <=98; i++)
      {
	_putchar(i);
	  }
    for (i = n; i >=98; i++)
      {
	_putchar(i);
      }
    _putchar(',');
    _putchar(' ');
}
