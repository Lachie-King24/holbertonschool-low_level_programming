#include "main.h"

/**
 * print_last_digit - prints last digit of a number
 * @r: number to check
 */

int print_last_digit(int r)
{
int last_digit;
last_digit = r % 10;
if (last_digit < 0)
{
return (-last_digit);
}
else
{
return (last_digit);
}
}
