#include "main.h"

/**
 * print_most_numbers - prints numbers from 0-9, followed by a new lines, not number 2 or 4
 *
 * Return: Result of function
 */

void print_most_numbers(void)
{
int a;
for (a = '0'; a <= 9; a++)
if (a = 52 || a = 50)
{
return (0);
}
else
{
_putchar(a);
}
_putchar('\n');
}
